#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @s: string.
 *
 * Return: length.
 */

size_t _strlen(const char *s)

{

int counter;

for (counter = 0, *s != '\0', s++);
counter++;

return (counter);
}
